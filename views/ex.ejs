<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <title>Travel Help Chat</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        <style>
            *{
                padding:0;
                margin: 0;
                box-sizing: border-box;
                font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            }
            body{
                background-color: #f8f9fa;
            }
            .chat-container{
                max-width: 800px;
                margin: 20px auto;
                background: white;
                border-radius: 15px;
                height: 90vh;
                box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
            }
            .chat-header{
                padding: 20px;
                color: white;
                background: #d72d6e;
                border-radius: 15px 15px 0 0;
            }
            .chat-messages{
                height: calc(100%-120px);
                overflow-y: auto;
                padding: 20px;
                display: flex;
                flex-direction: column;
                gap: 15px;
            }
            .message{
                max-width: 80%;
                padding: 15px 20px;
                border-radius: 15px;
                position: relative;
            }
            .bot-message{
                background:#e9ecef;
                align-self: flex-start;
            }
            .user-message{
                color: white;
                background: #d72d6e;
                align-self: flex-end;
            }
            .quick-replies {
                display: flex;
                gap: 10px;
                margin-top: 10px;
                flex-wrap: wrap;
            }
            .quick-option {
                background: white;
                border: 2px solid #d72d6e;
                color: #d72d6e;
                padding: 8px 15px;
                border-radius: 20px;
                cursor: pointer;
                transition: all 0.3s;
                font-size: 0.9em;    
            }

            .quick-option:hover {
                background: #d72d6e;
                color: white;
            }

            .timestamp {
                font-size: 0.75em;
                opacity: 0.7;
                margin-top: 5px;
            }

            .back-button {
                background: #6c757d;
                color: white;
                border: none;
            }

            @media (max-width: 768px) {
            .chat-container {
                margin: 10px;
                height: 95vh;
            }
            
            .quick-option {
                font-size: 0.8em;
                padding: 6px 12px;
            }
            }
        </style>
    </head>
    <body>
        <div class="chat-container">
            <div class="chat-header">
                <h2>Travel Booking Support</h2>
                <p>We are here to help 24/7</p>
            </div>
            <div class="chat-messages" id="chatMessages">
                <!---- Chat messages--->
            </div>
            <script type="module" src="/scripts/ex.js"></script>
        </div>
    </body>
</html>